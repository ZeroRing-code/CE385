// ตรวจสอบสิทธิ์การเข้าสู่ระบบ
function login(inputUser, inputPass, role, isActive, age) {
  const correctUser = "admin";
  const correctPass = "ce385pass";

  // ตรวจตัวตนก่อนตรวจสิทธิ์
  if (inputUser !== correctUser || inputPass !== correctPass) {
    return "ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง";
  }

  // ตรวจสถานะบัญชี
  if (!isActive) {
    return "บัญชีนี้ถูกระงับการใช้งาน";
  }

  // ตรวจอายุผู้ใช้งาน
  if (age < 18) {
    return "อายุไม่ถึงเกณฑ์";
  }

  // ตรวจบทบาทผู้ใช้งาน
  if (role === "อาจารย์") {
    return "เข้าสู่ระบบสำเร็จ (สิทธิ์ผู้ดูแล)";
  } else if (role === "นักศึกษา") {
    return "เข้าสู่ระบบสำเร็จ (สิทธิ์ทั่วไป)";
  } else {
    return "เข้าสู่ระบบสำเร็จ (สิทธิ์ไม่ระบุ)";
  }
}

// ทดสอบกรณีต่าง ๆ
console.log("1. สำเร็จ (อาจารย์) :", login("admin", "ce385pass", "อาจารย์", true, 30));
console.log("2. สำเร็จ (นักศึกษา) :", login("admin", "ce385pass", "นักศึกษา", true, 20));
console.log("3. รหัสผ่านผิด      :", login("admin", "wrongpass", "นักศึกษา", true, 20));
console.log("4. ชื่อผู้ใช้ผิด     :", login("user", "ce385pass", "นักศึกษา", true, 20));
console.log("5. บัญชีถูกระงับ    :", login("admin", "ce385pass", "นักศึกษา", false, 20));
console.log("6. อายุไม่ถึง        :", login("admin", "ce385pass", "นักศึกษา", true, 16));

/*
==================================================================
คำตอบคำถามท้ายข้อ
==================================================================
1. ทำไมต้องตรวจ username/password ก่อนตรวจ role?
ตอบ: เพื่อทำเรื่อง Authentication (ยืนยันตัวตน) ก่อนทำ Authorization (ตรวจสอบสิทธิ์) 
     ระบบจำเป็นต้องพิสูจน์ก่อนว่าผู้ใช้คือใครจริงหรือไม่ ก่อนที่จะเริ่มอ่านหรือตรวจสอบบทบาทสิทธิ์การใช้งาน

2. ถ้าย้ายการตรวจ "อายุไม่ถึงเกณฑ์" เป็นข้อแรก จะเกิดปัญหาอะไร?
ตอบ: จะเกิดปัญหาช่องโหว่ด้านความปลอดภัย (Information Disclosure) เพราะคนที่ยังไม่ได้พิสูจน์ตัวตน
     สามารถเดาหรือตรวจสอบอายุของผู้ใช้งานในระบบได้ทันที โดยที่ยังไม่ต้องป้อนรหัสผ่านให้ถูกต้อง
*/